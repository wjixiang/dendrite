[package]
name = "llm_api"
version = "0.3.0"
edition = "2024"
authors = ["Wjixiang", "Sonnet 4.0 + Cursor"]
description = "Comprehensive, type-safe Rust SDK for the Anthropic API with streaming, tools, vision, files, and batch processing support."
documentation = "https://docs.rs/llm_api"
repository = "https://github.com/dimichgh/agentik"
license = "MIT"
keywords = ["anthropic", "claude", "ai", "api", "sdk"]
categories = ["api-bindings", "web-programming::http-client", "asynchronous"]
readme = "README.md"
exclude = ["target/", ".git/", ".github/", "*.sh", ".DS_Store"]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
targets = ["x86_64-unknown-linux-gnu"]

[lib]
name = "llm_api"
path = "src/lib.rs"

[dependencies]
types = { version = "0.1.0", path = "../types", features = ["http"] }
reqwest = { version = "0.12", features = ["json", "stream", "multipart"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["rt-multi-thread", "macros"] }
futures = { version = "0.3", features = ["alloc"] }
thiserror = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
dotenvy = "0.15"
url = "2.5"
bytes = "1.5"
mime = "0.3.17"
tokio-stream = { version = "0.1", features = ["sync"] }
eventsource-stream = "0.2"
pin-project = "1.0"
async-trait = "0.1"
tempfile = "3.0"
sha2 = "0.10"
base64 = "0.22"
backoff = "0.4"
tower = "0.4"
once_cell = "1.0"
chrono = { version = "0.4", features = ["serde"] }
mockall = "0.14"

[features]
default = []

[dev-dependencies]
tokio-test = "0.4"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
chrono = "0.4"
