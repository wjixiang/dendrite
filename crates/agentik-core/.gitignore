# Build artifacts
target

# Cargo lockfile: not needed for libraries (consumers pin via their own lockfile)
Cargo.lock

# Backup / editor scratch
**/*.rs.bk
*.swp
*.swo
*~
.DS_Store

# IDE
.idea/
.vscode/
*.iml
